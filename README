Brink Developer Aggregator

Running on brink.immersedcode.org

Currently requires Werkzeug==dev

Things to do:

  1. create virtualenv
  2. install dependencies with `pip install -r requirements.txt`
  3. create a local.cfg:

    SQLALCHEMY_DATABASE_URI = 'sqlite:////path/to/brink.db'
    FORUM_USERNAME = '...'
    FORUM_PASSWORD = '...'
